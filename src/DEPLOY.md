# Інструкція з деплою на GitHub Pages

## Автоматичний деплой (рекомендовано)

### Крок 1: Налаштування GitHub Pages

1. Перейдіть у ваш репозиторій на GitHub: `https://github.com/Katywenkatwins/Rezerv`
2. Натисніть **Settings** (Налаштування)
3. У лівому меню виберіть **Pages**
4. У розділі **Build and deployment**:
   - **Source**: виберіть **GitHub Actions** (замість Deploy from a branch)
5. Збережіть налаштування

### Крок 2: Завантажте файли на GitHub

Завантажте ці нові файли у ваш репозиторій:
- `vite.config.ts`
- `package.json`
- `tsconfig.json`
- `tsconfig.node.json`
- `src/main.tsx`
- `.github/workflows/deploy.yml`

### Крок 3: Автоматичний деплой

Після завантаження файлів на GitHub:
1. Перейдіть у вкладку **Actions** вашого репозиторію
2. GitHub автоматично запустить workflow "Deploy to GitHub Pages"
3. Зачекайте, поки workflow завершиться (зелена галочка)
4. Ваш сайт буде доступний за адресою: `https://katywenkatwins.github.io/Rezerv/`

### Крок 4: Перевірка

Відкрийте `https://katywenkatwins.github.io/Rezerv/` у браузері - додаток має працювати!

---

## Ручний деплой (якщо потрібно)

### Попередні вимоги:
- Node.js версії 18 або вище
- Git

### Кроки:

1. **Клонуйте репозиторій (якщо ще не зробили):**
```bash
git clone https://github.com/Katywenkatwins/Rezerv.git
cd Rezerv
```

2. **Встановіть залежності:**
```bash
npm install
```

3. **Зберіть проєкт:**
```bash
npm run build
```

Це створить папку `dist` з готовими файлами.

4. **Завантажте зміни на GitHub:**
```bash
git add .
git commit -m "Add build configuration"
git push origin main
```

5. **GitHub Actions автоматично задеплоїть сайт**

---

## Важливі файли для деплою

### `vite.config.ts`
Конфігурація Vite з правильним base path для GitHub Pages:
- `base: '/Rezerv/'` - це важливо для правильної роботи на GitHub Pages

### `.github/workflows/deploy.yml`
GitHub Actions workflow, який автоматично:
- Встановлює залежності
- Збирає проєкт
- Деплоїть на GitHub Pages

### `package.json`
Містить всі залежності та скрипти для збірки проєкту.

---

## Troubleshooting (Вирішення проблем)

### Проблема: Сайт показує білу сторінку

**Рішення:**
1. Перевірте консоль браузера (F12 → Console)
2. Якщо бачите помилки 404 для CSS/JS файлів:
   - Переконайтеся, що у `vite.config.ts` вказано `base: '/Rezerv/'`
   - Перезберіть та задеплойте знову

### Проблема: GitHub Actions не запускається

**Рішення:**
1. Перейдіть у Settings → Actions → General
2. Переконайтеся, що **Actions permissions** дозволені
3. Переконайтеся, що у **Workflow permissions** вибрано "Read and write permissions"

### Проблема: Build не вдається

**Рішення:**
1. Перевірте, чи всі файли завантажені на GitHub
2. Перевірте логи у вкладці **Actions**
3. Переконайтеся, що `package.json` містить всі необхідні залежності

### Проблема: PWA не працює

**Рішення:**
PWA вимагає HTTPS, який автоматично надається GitHub Pages. Але:
1. Переконайтеся, що всі іконки завантажені у папку `/public`
2. Очистіть кеш браузера
3. Спробуйте знову "Додати на головний екран"

---

## Локальне тестування перед деплоєм

### Запуск в режимі розробки:
```bash
npm run dev
```
Відкрийте http://localhost:3000

### Перегляд production білду локально:
```bash
npm run build
npm run preview
```
Відкрийте http://localhost:4173

---

## Оновлення сайту

Після будь-яких змін у коді:

1. Збережіть зміни локально
2. Закомітьте та запуште на GitHub:
```bash
git add .
git commit -m "Опис змін"
git push origin main
```
3. GitHub Actions автоматично перебудує та задеплоїть сайт

---

## Перевірка статусу деплою

1. Перейдіть у вкладку **Actions** на GitHub
2. Знайдіть останній workflow run
3. Перевірте статус:
   - 🟢 Зелена галочка - успішно
   - 🔴 Червоний хрестик - помилка (клікніть для деталей)
   - 🟡 Жовте коло - в процесі

---

## Корисні посилання

- Ваш сайт: https://katywenkatwins.github.io/Rezerv/
- GitHub репозиторій: https://github.com/Katywenkatwins/Rezerv
- GitHub Actions: https://github.com/Katywenkatwins/Rezerv/actions
- GitHub Pages налаштування: https://github.com/Katywenkatwins/Rezerv/settings/pages

---

## Контакт

Якщо виникають проблеми:
1. Перевірте логи у GitHub Actions
2. Перевірте консоль браузера на помилки
3. Переконайтеся, що всі файли правильно завантажені
