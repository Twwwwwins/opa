# 🎨 Гайд по пушу з Figma Make на GitHub

## ⚠️ ВАЖЛИВА ІНФОРМАЦІЯ

Коли ви пушите проект через Figma Make на GitHub, він створює таку структуру:
```
Rezervplus/
├── index.html          ← Створюється автоматично Figma
├── package.json        ← Створюється автоматично Figma
├── vite.config.ts      ← Створюється автоматично Figma
├── tsconfig.json       ← Створюється автоматично Figma
└── src/                ← Весь ваш проект тут
    ├── App.tsx
    ├── main.tsx
    ├── components/
    ├── styles/
    └── ...
```

Це нормально! Я оновив workflow щоб він працював з такою структурою.

---

## 📋 **ЩО ПОТРІБНО ЗРОБИТИ:**

### **Крок 1: Перевірити файли які Figma створює автоматично**

Після того як Figma запушить проект, перевірте на GitHub ці 4 файли:

#### ✅ `index.html` має містити:
```html
<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Резерв ID</title>
  <link rel="manifest" href="/manifest.json">
  <meta name="theme-color" content="#e0dfcb">
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.tsx"></script>
</body>
</html>
```

#### ✅ `vite.config.ts` має містити:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: '/Rezervplus/',  // ← ВАЖЛИВО!
  build: {
    outDir: 'dist',
  },
});
```

#### ✅ `package.json` має містити:
```json
{
  "name": "rezerv-id",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "lucide-react": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.2.0"
  }
}
```

---

## 🔧 **Крок 2: Якщо Figma створює неправильні файли**

### **Варіант А: Редагувати на GitHub вручну**

1. Перейдіть на https://github.com/Katywenkatwins/Rezervplus
2. Відкрийте файл `vite.config.ts`
3. Натисніть **кнопку олівця** (Edit)
4. Знайдіть рядок `base:` і змініть на:
   ```typescript
   base: '/Rezervplus/',
   ```
5. Знайдіть рядок `outDir:` і змініть на:
   ```typescript
   outDir: 'dist',
   ```
6. **Commit changes**

### **Варіант Б: Створити ці файли в Figma Make**

В Figma Make створіть ці файли В КОРЕНІ проекту (поруч з App.tsx):

**`/vite.config.ts`** (якщо його немає):
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: '/Rezervplus/',
  build: {
    outDir: 'dist',
  },
});
```

**`/index.html`** (якщо його немає - але він вже є):
Залишити як є

**`/package.json`** (якщо його немає):
Залишити як є

---

## 📁 **Крок 3: Перевірити папку `.github`**

**ВАЖЛИВО:** Переконайтеся що на GitHub є папка `.github/workflows/` з файлом `deploy.yml`

### Як перевірити:

1. Перейдіть на https://github.com/Katywenkatwins/Rezervplus
2. Шукайте папку `.github` (вона може бути прихована)
3. Всередині має бути `workflows/deploy.yml`

### Якщо папки немає:

Створіть її ВРУЧНУ на GitHub:

1. На головній сторінці репозиторію натисніть **"Add file"** → **"Create new file"**
2. В полі імені файлу напишіть: `.github/workflows/deploy.yml`
3. Вставте вміст (я створив вище)
4. **Commit new file**

---

## ⚙️ **Крок 4: Налаштувати GitHub Pages**

1. **Settings** → **Pages**
2. **Source:** виберіть **"GitHub Actions"** (НЕ "Deploy from a branch")
3. Збережіть

---

## ▶️ **Крок 5: Запустити деплой**

1. Перейдіть на вкладку **Actions**
2. Якщо бачите помилку з попереднього запуску - ігноруйте
3. Натисніть **"Deploy to GitHub Pages"** в лівому меню
4. Натисніть **"Run workflow"** → **"Run workflow"**
5. Зачекайте 2-3 хвилини

---

## 🐛 **Debugging: Як перевірити що пішло не так**

### 1. Перевірити структуру на GitHub:
```
Rezervplus/
├── .github/
│   └── workflows/
│       └── deploy.yml     ← МАЄ БУТИ ТУТ!
├── src/
│   ├── App.tsx
│   ├── main.tsx
│   ├── components/
│   ├── styles/
│   └── public/           ← Або public може бути в корені
├── index.html
├── package.json
└── vite.config.ts
```

### 2. Перевірити GitHub Actions logs:

1. **Actions** → клікніть на останній запуск
2. Клікніть на **"build"** job
3. Дивіться логи - там буде написано що пішло не так

### 3. Типові помилки:

| Помилка | Рішення |
|---------|---------|
| `Cannot find module 'vite'` | Додати `vite` в `devDependencies` в `package.json` |
| `404 (не знаходить файли)` | Змінити `base: '/'` на `base: '/Rezervplus/'` в `vite.config.ts` |
| `esbuild not found` | Workflow автоматично виправить |
| Білий екран | Неправильний `base` path в `vite.config.ts` |

---

## 📱 **PWA файли**

Переконайтеся що в папці `src/public/` (або просто `public/`) є:

```
public/
├── manifest.json          ← PWA manifest
├── service-worker.js      ← Service Worker
├── favicon.svg            ← Іконка
└── icon-placeholder.svg   ← Placeholder для іконки
```

Якщо їх немає - вони вже є в проекті Figma Make, просто переконайтеся що вони запушились.

---

## ✅ **Чеклист перед деплоєм:**

- [ ] `.github/workflows/deploy.yml` існує на GitHub
- [ ] `vite.config.ts` має `base: '/Rezervplus/'`
- [ ] `vite.config.ts` має `outDir: 'dist'`
- [ ] Settings → Pages → Source: "GitHub Actions"
- [ ] Папка `public/` з PWA файлами існує
- [ ] `package.json` має скрипт `"build": "vite build"`

---

## 🎉 **Після успішного деплою:**

Ваш сайт буде доступний на:
**https://katywenkatwins.github.io/Rezervplus/**

PWA можна встановити на телефон:
- **Android Chrome:** Меню → "Install app"
- **iOS Safari:** Share → "Add to Home Screen"

---

## 💡 **Підказка:**

Після кожного пушу через Figma Make, GitHub Actions автоматично **задеплоїть зміни**. Не потрібно нічого робити вручну!

Просто зачекайте 2-3 хвилини після пушу і оновіть сторінку.
