# 📋 Інструкції для деплою на GitHub Pages

## ✅ Виправлені файли (оновіть на GitHub):

### 1. `/index.html`
- Замінено абсолютні шляхи на відносні
- Посилання на `manifest.json`, іконки та `app.js` тепер без `/Rezerv/` префіксу

### 2. `/public/manifest.json`
- Використовує відносні шляхи (`"start_url": "."`, `"scope": "./"`)
- Іконки посилаються відносно без префіксу

### 3. `/public/app.js`
- Автоматично визначає правильний шлях до Service Worker
- Працює як на localhost, так і на GitHub Pages

### 4. `/public/sw.js`
- Альтернативна точка входу для Service Worker

## 🚀 Кроки для деплою:

1. **Закомітьте всі зміни з цих файлів**
2. **Зачекайте на завершення GitHub Actions**
3. **Перевірте сайт**: https://katywenkatwins.github.io/Rezerv/

## 🔍 Як перевірити, що все працює:

1. Відкрийте https://katywenkatwins.github.io/Rezerv/
2. Відкрийте DevTools (F12)
3. Перейдіть на вкладку **Application** → **Manifest**
4. Має відображатися манифест без помилок
5. Перейдіть на **Application** → **Service Workers**
6. Service Worker має бути зареєстрований

## 📁 Структура файлів у `/public`:

```
/public
├── app.js              ← Реєстрація Service Worker
├── sw.js               ← Альтернативна точка входу
├── service-worker.js   ← Основний Service Worker
├── manifest.json       ← PWA маніфест
├── favicon.svg         ← Іконка
├── icon-128x128.png    ← PWA іконка 128x128
├── icon-152x152.png    ← iOS іконка 152x152
├── icon-180x180.png    ← iOS іконка 180x180
├── icon-192x192.png    ← PWA іконка 192x192
└── icon-512x512.png    ← PWA іконка 512x512
```

## ⚙️ Як працює base path:

- **Під час розробки** (localhost): файли доступні з `/`
- **Під час білду**: Vite додає `/Rezerv/` до всіх шляхів автоматично завдяки `base: '/Rezerv/'` в `vite.config.ts`
- **Результат**: файли працюють в обох режимах без змін коду

## 🐛 Якщо виникли проблеми:

1. Очистіть кеш браузера (Ctrl+Shift+Del)
2. Зробіть Hard Refresh (Ctrl+Shift+R)
3. Перевірте, що GitHub Actions завершився успішно
4. Подивіться консоль DevTools на наявність помилок
