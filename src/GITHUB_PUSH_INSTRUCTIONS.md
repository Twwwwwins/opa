# 📦 Інструкція по деплою на GitHub Pages

## ⚠️ ВАЖЛИВО: Видаліть старі файли з GitHub!

На GitHub у вас старі файли з неправильною структурою. Потрібно їх видалити.

---

## 🗑️ Крок 1: Очистити GitHub репозиторій

### Варіант А: Через GitHub (легший)

1. Перейдіть на https://github.com/Katywenkatwins/Rezervplus
2. Натисніть **Settings** (вкладка вгорі)
3. Прокрутіть вниз до **"Danger Zone"**
4. Натисніть **"Delete this repository"**
5. Підтвердіть видалення
6. Створіть новий репозиторій з назвою **"Rezervplus"**

### Варіант Б: Через Git (складніший)

```bash
# Очистити всю історію
git checkout --orphan new-main
git add -A
git commit -m "Initial commit with correct structure"
git branch -D main
git branch -m main
git push -f origin main
```

---

## 📤 Крок 2: Завантажити файли з Figma Make на GitHub

### Копіюйте ВСІ ці файли/папки:

```
✅ /components/         (папка)
✅ /imports/            (папка)
✅ /public/             (папка)
✅ /src/                (папка)
✅ /styles/             (папка)
✅ /.github/            (папка - ОБОВ'ЯЗКОВО!)
✅ /App.tsx
✅ /index.html
✅ /package.json
✅ /vite.config.ts
✅ /tsconfig.json
✅ /tsconfig.node.json
✅ /.gitignore
✅ /README.md
```

### ❌ НЕ копіюйте:

```
❌ /workflows/ (не потрібно, є /.github/workflows/)
❌ Всі .md файли крім README.md
❌ node_modules/ (якщо є)
```

---

## 🚀 Крок 3: Запушити на GitHub

### 1. Ініціалізувати Git (якщо ще не зробили)

```bash
git init
git add .
git commit -m "Initial commit: Rezerv ID PWA"
```

### 2. Підключити до GitHub

```bash
git remote add origin https://github.com/Katywenkatwins/Rezervplus.git
git branch -M main
git push -u origin main
```

### 3. Перевірити що файли на місці

Перейдіть на GitHub і переконайтеся що структура така:

```
Rezervplus/
├── .github/
│   └── workflows/
│       └── deploy.yml     ← ОБОВ'ЯЗКОВО ТУТ!
├── components/
├── imports/
├── public/
│   ├── manifest.json
│   ├── service-worker.js
│   └── favicon.svg
├── src/
│   └── main.tsx
├── styles/
├── App.tsx
├── index.html             ← Оновлений з правильними шляхами
├── package.json           ← Правильний (назва "rezerv-id")
├── vite.config.ts         ← Оновлений (base: '/Rezervplus/')
├── tsconfig.json
└── .gitignore
```

---

## ⚙️ Крок 4: Налаштувати GitHub Pages

1. Перейдіть на **Settings** → **Pages**
2. **Source:** виберіть **"GitHub Actions"** (НЕ "Deploy from a branch")
3. Збережіть зміни

---

## ▶️ Крок 5: Запустити деплой

1. Перейдіть на вкладку **Actions**
2. Виберіть **"Deploy to GitHub Pages"** зліва
3. Натисніть **"Run workflow"** → **"Run workflow"**
4. Зачекайте 2-3 хвилини
5. Перейдіть на https://katywenkatwins.github.io/Rezervplus/

---

## ✅ Перевірка PWA

Після деплою перевірте:

1. **Відкрийте сайт на телефоні**
2. **Chrome:** Меню → "Install app" або "Add to Home Screen"
3. **Safari (iOS):** Кнопка "Share" → "Add to Home Screen"
4. **Іконка додатку** має з'явитися на робочому столі
5. **Відкрийте додаток** - він має працювати як нативний (без браузерної панелі)

---

## 🔧 Якщо щось не працює

### Проблема: Білий екран на GitHub Pages

**Рішення:** Перевірте що в `vite.config.ts`:
```javascript
base: '/Rezervplus/'  // Має бути ТОЧНО назва репозиторію!
```

### Проблема: PWA не встановлюється

**Рішення:** 
1. Перевірте що `manifest.json` доступний: https://katywenkatwins.github.io/Rezervplus/manifest.json
2. Перевірте консоль браузера (F12) на помилки
3. PWA працює тільки на HTTPS (GitHub Pages використовує HTTPS автоматично)

### Проблема: GitHub Actions не запускається

**Рішення:**
1. Перевірте що папка `.github/workflows/` існує (з КРАПКОЮ на початку!)
2. Файл має називатися `deploy.yml`
3. Settings → Pages → Source має бути "GitHub Actions"

---

## 📱 Структура для PWA (вже налаштовано)

- ✅ `manifest.json` - з правильними шляхами `/Rezervplus/`
- ✅ `service-worker.js` - оновлений BASE_PATH
- ✅ `index.html` - meta tags для PWA
- ✅ Іконки в `/public/` (згенеруйте за потреби)
- ✅ `vite.config.ts` - правильний publicDir

---

## 🎉 Готово!

Після виконання всіх кроків у вас буде:
- ✅ Робочий сайт на GitHub Pages
- ✅ PWA який можна встановити на телефон
- ✅ Автоматичний деплой при кожному push
- ✅ Offline підтримка (service worker)

**URL:** https://katywenkatwins.github.io/Rezervplus/
