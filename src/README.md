# Резерв ID - Військово-обліковий документ

Цифровий військово-обліковий документ з PWA підтримкою.

## 🚀 Швидкий старт

### Локальна розробка:

```bash
npm install
npm run dev
```

### Білд для продакшену:

```bash
npm run build
npm run preview
```

## 📱 Генерація іконок PWA

**Важливо!** Перед деплоєм на GitHub Pages потрібно згенерувати іконки:

1. Відкрийте файл `/public/generate-icons.html` в браузері
2. Натисніть "Завантажити всі іконки"
3. Збережіть всі згенеровані файли в папку `/public`

Альтернативно, використайте онлайн сервіс:
- https://www.pwabuilder.com/imageGenerator

## 📦 Деплой на GitHub Pages

1. Переконайтеся що всі іконки створені в `/public`
2. Закомітьте всі зміни:
   ```bash
   git add .
   git commit -m "Update PWA icons and manifest"
   git push origin main
   ```
3. GitHub Actions автоматично задеплоїть ваш сайт
4. Сайт буде доступний за адресою: https://katywenkatwins.github.io/Rezerv/

## 🛠️ Структура проекту

```
/
├── public/
│   ├── manifest.json       # PWA manifest
│   ├── app.js             # PWA registration
│   ├── sw.js              # Service Worker
│   ├── icon-*.png         # PWA іконки (потрібно згенерувати!)
│   └── favicon.svg        # Favicon
├── src/
│   ├── App.tsx            # Головний компонент
│   ├── main.tsx           # Entry point
│   └── styles/
│       └── globals.css    # Глобальні стилі
└── .github/
    └── workflows/
        └── deploy.yml     # GitHub Actions workflow

```

## ⚙️ Налаштування

### GitHub Pages:

1. Перейдіть в Settings → Pages
2. Source: **GitHub Actions**
3. Зачекайте завершення деплою

### Vite config:

Переконайтеся що `base` в `vite.config.ts` відповідає назві вашого репозиторію:

```typescript
export default defineConfig({
  base: '/Rezerv/', // Назва репозиторію з великої літери!
  // ...
});
```

## 🔧 Вирішення проблем

### Manifest не знайдено (404):

1. Переконайтеся що всі файли в `/public` закомічені
2. Перевірте чи завершився GitHub Actions workflow
3. Очистіть кеш браузера (Ctrl+Shift+Delete)
4. Зробіть Hard Refresh (Ctrl+Shift+R)

### PWA не встановлюється:

1. Переконайтеся що всі іконки створені
2. Перевірте manifest.json: https://katywenkatwins.github.io/Rezerv/manifest.json
3. Відкрийте DevTools → Application → Manifest
4. Перевірте Console на помилки

## 📄 Ліцензія

MIT
